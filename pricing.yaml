# Per-model pricing data for the cost tracker.
#
# After M1 Task 06, `pricing.yaml` carries only Claude Code CLI tiers —
# LiteLLM supplies the pricing table for Gemini and Ollama/Qwen routes
# per KDR-007 / architecture.md §4.1. M1 Task 08's CostTracker refit
# will revisit this file if sub-model / override entries are needed for
# `modelUsage` breakdown; until then the Claude CLI tiers are recorded
# at $0 because Claude Max bills via subscription, not per-token API.

pricing:
  # Claude Max CLI tiers — subscription-billed, zero per-token cost.
  claude-opus-4-7:
    input_per_mtok: 0.0
    output_per_mtok: 0.0
  claude-sonnet-4-6:
    input_per_mtok: 0.0
    output_per_mtok: 0.0
  claude-haiku-4-5-20251001:
    input_per_mtok: 0.0
    output_per_mtok: 0.0
