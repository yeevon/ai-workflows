# Writing a Workflow

> **Placeholder.** This document is to be authored by M3 Task 01 (Workflow Entry Points).
> It will describe how to compose components into a workflow, register it in
> `ai_workflows/workflows/`, and wire it to the `aiw` CLI.
